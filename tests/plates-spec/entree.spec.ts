/* eslint-disable max-len */
/*import 'mocha';
import {expect} from 'chai';


// Pruebas
describe('Pruebas para el ejercicio 1 - El combate definitivo', () => {
  it('pikachu.getUniverso() returns value Pokemon', () => {
    expect().to.be.equal();
  });
  it('goku.getUniverso() returns value DragonBall', () => {
    expect().to.be.equal();
  });
});

// Prueba
const pollo: Cereal = new Cereal("pollo", 80, 20, 12, 25, 32, 45, 87, 98, 14, "de aqui", "de alla");
const carne: Seed = new Seed("carne", 80, 20, 12, 25, 32, 45, 87, 98, 14, "de aqui", "de alla");
const azucar: Seed = new Seed("azucar", 80, 20, 12, 25, 32, 45, 87, 98, 14, "de aqui", "de alla");

const ingredientes = new Map<Aliment, number>();

ingredientes.set(pollo, 50);
ingredientes.set(carne, 80);
ingredientes.set(azucar, 60);

const entrante: Entree = new Entree("pabellon", ingredientes);

console.log(entrante.print());
*/