/*import 'mocha';
import {expect} from 'chai';
import {Menu} from '../../src/menu/menu';
import {SetMenu} from '../../src/menu/setmenu';
import {PersonalizedMenu} from '../../src/menu/personalizedmenu';

describe('ejercicio 1', () => {


    it('Menu es un objeto de tipo menu', () => {
        expect(1).to.be.equal(true);
    });

    it('setmenus es un objeto de tipo setmenu', () => {
        expect(1).to.be.equal(true);
    });

    it('custom_menu es un objeto de tipo PerzonalizedMenu', () => {
        expect(1).to.be.equal(true);
    });
    
});*/